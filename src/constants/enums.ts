export enum NavbarAppearance {
  FIXED = "fixed",
  UPWARDS_SCROLL = "upwards-scroll",
  DEFAULT = "default",
}

export enum MediaFormat {
  IMAGE = "image",
  VIDEO = "video",
}

export enum MediaSlideLayout {
  DEFAULT = "default",
  HORIZONTAL = "horizontal",
  VERTICAL = "vertical",
}
